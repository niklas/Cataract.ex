import quantify from '../../../utils/quantify';
import { module, test } from 'qunit';

module('Unit | Utility | quantify');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = quantify();
  assert.ok(result);
});
