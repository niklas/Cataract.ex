import { humanKiloBytes } from '../../../helpers/human-kilo-bytes';
import { module, test } from 'qunit';

module('Unit | Helper | human kilo bytes');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = humanKiloBytes(42);
  assert.ok(result);
});
