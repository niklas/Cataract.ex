<header>
  {{model.name}}
  <small>
    {{model.path}}
  </small>
</header>

<ul>
  {{#each model.directories as |directory|}}
  <li>{{directory.name}} <small>{{directory.path}}</small></li>
  {{/each}}
</ul>

{{outlet}}
